import {formSubmission} from './documents/formSubmission'
import {homePage} from './documents/homePage'
import {siteSettings} from './documents/siteSettings'

export const schemaTypes = [siteSettings, homePage, formSubmission]
